/* eslint-disable react/prop-types */

const Title = ({ title }) => {
    return (
        <div className="">
            <h2 className="title_lines text-2xl font-bold">{title}</h2>
        </div>

    )
}

export default Title